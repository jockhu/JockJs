/**
 *
 *
 * Javascript Api Service
 * @author Jock
 *
 *
 *
 */
(function(){
    var App = require('./apps/app').App;
    var app = new App();
    app.start();
})();


// start

// node service.js [[[[port] | debug] | compress] | version]
